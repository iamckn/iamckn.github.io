<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: vpn | The poetry of (in)security]]></title>
  <link href="http://www.ckn.io/blog/categories/vpn/atom.xml" rel="self"/>
  <link href="http://www.ckn.io/"/>
  <updated>2019-08-27T17:23:22+03:00</updated>
  <id>http://www.ckn.io/</id>
  <author>
    <name><![CDATA[ck]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Wireguard VPN: Portable Raspberry Pi Setup]]></title>
    <link href="http://www.ckn.io/blog/2017/12/28/wireguard-vpn-portable-raspberry-pi-setup/"/>
    <updated>2017-12-28T16:44:50+03:00</updated>
    <id>http://www.ckn.io/blog/2017/12/28/wireguard-vpn-portable-raspberry-pi-setup</id>
    <content type="html"><![CDATA[<p>We often use wireless networks to access the internet. This may be at home, work or even places like restaurants. When we connect to these networks, the security of our internet traffic is under the control of the owner of the wireless network. This is also the case when we connect to a wired connection on a network we don&rsquo;t control.</p>

<!--more-->


<p>With this in mind, using a VPN on foreign networks is a good idea. Configuring and remembering to turn on VPN on the several mobile devices we carry around is often a hassle.</p>

<p>In this post, I&rsquo;ll detail how you can set up a portable VPN connection on a Raspberry Pi. You can carry it with you everywhere you go and have all your devices connect to it ensuring a secure connection. This also saves the work of configuring a VPN connection on all your devices.</p>

<p>Before we continue, you can go through my post on setting up a typical Wireguard VPN connection <a href="https://www.ckn.io/blog/2017/11/14/wireguard-vpn-typical-setup/" target="_blank"><strong>here</strong></a>.</p>

<h1>Set up details</h1>

<p>Our intended setup is as below:</p>

<p><img src="/images/portable_vpn.png"></p>

<p>Here&rsquo;s how our setup will look like:</p>

<ul>
<li>An ubuntu 16.04 (x64) VPS as our VPN server (Gateway).</li>
<li>The internet facing interface on the server is eth0.</li>
<li>A Raspberry Pi 3 Model B running Raspbian as our portable VPN client.</li>
<li>The Pi will be connected to the internet via LAN (eth0) or an external USB wireless card (wlan1).</li>
<li>We will use the 10.200.200.0/24 subnet for the network between the Pi and the VPN Gateway.</li>
<li>We will use the 10.100.100.0/24 subnet for the wireless network that the Pi will host for the clients on wlan0.</li>
<li>We will use 10.200.200.1/24 as the VPN Gateway interface IP.</li>
<li>We will use 10.200.200.2/24 as the Pi VPN interface IP.</li>
<li>We will use 10.100.100.1/24 as the Pi wireless network interface (wlan0) IP.</li>
<li>Unbound DNS resolver for added security.</li>
</ul>


<h1>Set up steps</h1>

<ol>
<li>Install WireGuard on the VPN server.</li>
<li>Prepare the Pi and install dependencies.</li>
<li>Set up Wireguard on the Pi.</li>
<li>Set up the wireless network on the Pi.</li>
<li>Set up forwarding and NAT</li>
<li>Bring up the wireless network and test the setup.</li>
</ol>


<h1>1. Install WireGuard on the VPN server.</h1>

<p>This is straightforward if you have gone through my guide <a href="https://www.ckn.io/blog/2017/11/14/wireguard-vpn-typical-setup/" target="_blank"><strong>here</strong></a>. I&rsquo;ll therefore run through the automated ansible process.</p>

<pre><code class="bash">#Clone the repo onto your computer
git clone https://github.com/iamckn/wireguard_ansible

#Move into the repo directory
cd wireguard_ansible

#Edit the hosts file in that directory to change the IP to that of your VPN Gateway

#Begin the installation process by running
ansible-playbook wireguard.yml -u root -k -i hosts

#If you're using an SSH key for authentication run this instead
ansible-playbook wireguard.yml -u root -i hosts

#Give it a few minutes and the server set up will be complete.
</code></pre>

<p>The VPN gateway will be set up to use <a href="https://www.unbound.net/" target="_blank"><strong>unbound</strong></a> to provide secure DNS to the VPN network. We will however need to modify the unbound dns configuration to account for the wireless network the Pi will host.</p>

<p>Edit the file <strong>/etc/unbound/unbound.conf</strong> and add the following two lines to the file:</p>

<pre><code class="bash">#allow pi wireless network to use the unbound dns server
access-control: 10.100.100.0/24         allow

#protect the pi wireless network subnet from public internet names resolution attempts
private-address: 10.100.100.0/24
</code></pre>

<p>Restart the DNS server for the changes to take effect</p>

<pre><code class="bash">systemctl restart unbound
</code></pre>

<h1>2. Prepare the Pi and install dependencies.</h1>

<p>We now move to the Pi to install some required dependencies. First ensure that your Pi has the latest raspbian OS installed, then update it and install the following dependencies:</p>

<pre><code class="bash">sudo apt update &amp;&amp; sudo apt-get upgrade
sudo apt-get install hostapd dnsmasq libmnl-dev linux-headers-rpi build-essential git dnsutils bc raspberrypi-kernel-headers iptables-persistent
</code></pre>

<h1>3. Set up Wireguard on the Pi.</h1>

<p>We then set up Wireguard on the Pi. Install Wireguard from source as follows:</p>

<pre><code class="bash">git clone https://git.zx2c4.com/WireGuard
cd WireGuard/src
make
sudo make install
sudo modprobe wireguard
</code></pre>

<p>Copy the file named <strong>wg0.conf</strong> from the home folder of the VPN server to the Pi. Use scp or whatever other method you prefer then move it to <strong>/etc/wireguard/wg0.conf</strong> on the Pi.</p>

<p>Bring up the Wireguard interface on the Pi and enable it to start on boot:</p>

<pre><code class="bash">sudo wg-quick up wg0
sudo systemctl enable wg-quick@wg0.service
</code></pre>

<p>The VPN tunnel between the Pi and the VPN Server should now be up and running. You can confirm this by checking the public IP on the Pi using the following command:</p>

<pre><code class="bash">curl ifconfig.co
</code></pre>

<h1>4. Set up the wireless network on the Pi.</h1>

<p>We now need to set up the Pi to host a wireless network through which other clients can connect. We will use <strong>hostapd</strong> to run the wireless network and <strong>dnsmasq</strong> for DNS and DHCP.</p>

<p>Edit the following line in the file <strong>/etc/default/hostapd</strong> as follows:</p>

<pre><code class="bash">DAEMON_CONF="/etc/hostapd/hostapd.conf"
</code></pre>

<p>Create the following file <strong>/etc/hostapd/hostapd.conf</strong> and edit it as follows:</p>

<pre><code class="bash">interface=wlan0
hw_mode=g
channel=10
ieee80211d=1
country_code=BZ
ieee80211n=1
wmm_enabled=1

ssid=&lt;your ssid&gt;
auth_algs=1
wpa=2
wpa_key_mgmt=WPA-PSK  
rsn_pairwise=CCMP
wpa_passphrase=&lt;password of the ssid&gt;
</code></pre>

<p>Modify the field <strong>ssid</strong> and <strong>wpa_passphrase</strong> to the name you want to use for your wireless network and the wireless password respectively.</p>

<p>Next we set up the various network interfaces on the Pi by editing the file <strong>/etc/network/interfaces</strong> and adding the following:</p>

<pre><code class="bash">auto wlan0
iface wlan0 inet static
  address 10.100.100.1
  netmask 24

allow-hotplug eth0
iface eth0 inet dhcp

allow-hotplug usb0
iface usb0 inet dhcp

allow-hotplug wlan1
iface wlan1 inet dhcp
   wpa-driver wext
   wpa-conf /etc/wpa_supplicant/wpa_supplicant.conf
</code></pre>

<p><strong>wlan0</strong> is set to the IP 10.100.100.1/24 and is the gateway that will be used by wireless clients connecting to the Pi.
All the other interfaces are set up as possible internet facing interfaces depending on which one is connected to the internet.</p>

<p>You may need to connect your Pi to different wireless networks using an external wireless USB card. If not, skip the following step, otherwise edit the file <strong>/etc/wpa_supplicant/wpa_supplicant.conf</strong> and add the following:</p>

<pre><code class="bash">network={
    ssid="network 1"
    psk="password to network 1"
    id_str="w"
    priority=1
}

network={
    ssid="network 2"
    psk="password to network 2"
    id_str="z"
}
</code></pre>

<p>You can add all the wireless networks you need to connect to to the file following the same format. Whenever you plug in an external wireless USB card, the Pi will scan for available networks and attempt to connect using the details in the file.</p>

<p>Let&rsquo;s now set up DHCP and DNS to serve the wireless network the clients connecting to the Pi will use.</p>

<p>We are going to use dnsmasq so let&rsquo;s first disable operation of the default raspbian dhcp server on the wlan0 interface.
Edit the file <strong>/etc/dhcpcd.conf</strong> and add the following line:</p>

<pre><code class="bash">denyinterfaces wlan0
</code></pre>

<p>We next back up the current dnsmasq configuration file:</p>

<pre><code class="bash">sudo mv /etc/dnsmasq.conf /etc/dnsmasq.conf.orig 
</code></pre>

<p>We then configure dns by recreating the file <strong>/etc/dnsmasq.conf</strong> and editing it as:</p>

<pre><code class="bash">dhcp-authoritative
interface=wlan0
listen-address=10.100.100.1
dhcp-range=10.100.100.50,10.100.100.150,12h
read-ethers
bogus-priv
domain-needed
dhcp-option=option:dns-server,10.200.200.1
</code></pre>

<p>The <strong>dhcp-range</strong> option determines the range of IPs clients connecting to the Pi will be allocated so you can modify it to suit your needs. Also note that the <strong>dns-server</strong> option is set to the VPN Server (Gateway) interface that we set up earlier.</p>

<h1>5. Set up forwarding and NAT</h1>

<p>To enable wireless clients to access the internet through the VPN connection between the Pi and the VPN Server, we need to do the following:</p>

<p>Uncomment the following line in <strong>/etc/sysctl.conf</strong></p>

<pre><code class="bash">net.ipv4.ip_forward=1
</code></pre>

<p>Then run the following commands:</p>

<pre><code class="bash">sudo sysctl -p
echo 1 &gt; /proc/sys/net/ipv4/ip_forward
</code></pre>

<p>Finally set up the necessary NAT rules and make them persistent:</p>

<pre><code class="bash">sudo iptables -t nat -A POSTROUTING -o wg0 -j MASQUERADE
sudo iptables -A FORWARD -i wlan0 -o wg0 -j ACCEPT
sudo iptables -A FORWARD -i wg0 -o wlan0 -m state --state RELATED,ESTABLISHED -j ACCEPT

sudo systemctl enable netfilter-persistent
sudo netfilter-persistent save
</code></pre>

<h1>6. Bring up the wireless network and test the setup.</h1>

<p>We now complete the network by starting the necessary services and bringing up the wireless network.</p>

<pre><code class="bash">sudo ifup wlan0
sudo service dnsmasq start
sudo service hostapd start
sudo update-rc.d hostapd enable 
</code></pre>

<p>We are finally done!</p>

<p>Test the set up to ensure everything works. Start with a test of DNS operation:</p>

<pre><code class="bash">dig www.google.com 10.200.200.1
</code></pre>

<p>Then check to see if the wireless network you set up is available and connect to it with a wireless client. If all went well you should have a secure VPN connection from your wireless client, to the Pi and then through the VPN server (Gateway). Don&rsquo;t forget to run a DNS leak test on <a href="http://dnsleak.com/.">http://dnsleak.com/.</a></p>

<p>You now have a portable secure VPN setup on your Pi that you can carry around and use. Just connect the Pi to the network through the LAN interface, external wireless USB card or even USB ethernet. Your devices can then connect to the VPN through the Pi&rsquo;s wireless network hosted on it&rsquo;s internal wireless interface (wlan0).</p>

<p>If you prefer to do a similar setup with everything happening over ipv6, refer to this great write-up <a href="https://danrl.com/blog/2016/travel-wifi/.">https://danrl.com/blog/2016/travel-wifi/.</a></p>

<p>That concludes the VPN series!</p>

<p>Till next time, happy hacking!</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Wireguard VPN: Chained Setup]]></title>
    <link href="http://www.ckn.io/blog/2017/12/28/wireguard-vpn-chained-setup/"/>
    <updated>2017-12-28T07:33:38+03:00</updated>
    <id>http://www.ckn.io/blog/2017/12/28/wireguard-vpn-chained-setup</id>
    <content type="html"><![CDATA[<p>Moving on from my previous <a href="https://www.ckn.io/blog/2017/11/14/wireguard-vpn-typical-setup/" target="_blank"><strong>post</strong></a> about setting up a typical Wireguard VPN connection, let&rsquo;s go through how to do a chained setup. I will show how to do both the typical chained Wireguard VPN connection and the one with selective routing as described in my earlier post <a href="https://www.ckn.io/blog/2017/11/12/vpns-an-opsec-primer/" target="_blank"><strong>here</strong></a></p>

<!--more-->


<h1>Set up details</h1>

<p>Let us start with the typical Wireguard VPN chained connection.</p>

<p><img src="/images/chained_vpn.png"></p>

<p>Here&rsquo;s how our setup will look like:</p>

<ul>
<li>An ubuntu 16.04 (x64) VPS as our first VPN server which we will refer to as the middleman (VPN Gateway One as shown in the diagram above).</li>
<li>An ubuntu 16.04 (x64) VPS as our second VPN server which we will refer to as the gate (VPN Gateway Two as shown in the diagram above).</li>
<li>An ubuntu 16.04 (x64) computer as the client.</li>
<li>The internet facing interface on both VPN servers is eth0.</li>
<li>We will use the 10.200.200.0/24 subnet for the network between the client and the middleman.</li>
<li>We will use the 10.100.100.0/24 subnet for the network between the middleman and the gate.</li>
<li>We will use 10.200.200.1/24 as the middleman client facing interface (wg0) IP.</li>
<li>We will use 10.200.200.2/24 as the VPN client interface (vpn0) IP .</li>
<li>We will use 10.100.100.1/24 as the gate VPN interface (wg0) IP.</li>
<li>We will use 10.100.100.2/24 as the middleman gate facing interface (gate0) IP.</li>
<li>Unbound DNS resolver for added security.</li>
</ul>


<h1>Set up steps</h1>

<ol>
<li>Install Wireguard on the middleman.</li>
<li>Install Wireguard on the gate.</li>
<li>Set up a Wireguard VPN tunnel between the client and the middleman.</li>
<li>Set up a Wireguard VPN tunnel between the middleman and the gate.</li>
<li>Configure policy routing on the middleman to route traffic from the client to the gate.</li>
<li>Update the middleman gate facing interface (gate0) to allow all traffic from the gate to be allowed in the tunnel.</li>
<li>Confirm everything works as desired by doing a traceroute to the internet from the client.</li>
</ol>


<h1>1. Install Wireguard on the middleman.</h1>

<p>I already went into the specifics of a typical Wirguard VPN setup in my previous post, so I&rsquo;ll not go into the details here.</p>

<p>I have shared ansible scripts on <a href="https://github.com/iamckn/chained-wireguard-ansible" target="_blank"><strong>Github</strong></a> for the automation so I&rsquo;ll just run through the process here.</p>

<pre><code class="bash">#Clone the repo on the client
git clone https://github.com/iamckn/chained-wireguard-ansible

#Move into the middleman folder
cd chained-wireguard-ansible/middleman/

#Edit the hosts file in that directory to change the IP to that of your middleman VPS

#Begin the installation process by running
ansible-playbook wireguard.yml -u root -k -i hosts

#If you're using an SSH key for authentication run this instead
ansible-playbook wireguard.yml -u root -i hosts

#Give it a few minutes and the server set up will be complete.
</code></pre>

<h1>2. Install Wireguard on the gate.</h1>

<p>The installation process is as below.</p>

<pre><code class="bash">#Clone the repo on the client
git clone https://github.com/iamckn/chained-wireguard-ansible

#Move into the gate folder
cd chained-wireguard-ansible/gate/

#Edit the hosts file in that directory to change the IP to that of your gate VPS

#Begin the installation process by running
ansible-playbook wireguard.yml -u root -k -i hosts

#If you're using an SSH key for authentication run this instead
ansible-playbook wireguard.yml -u root -i hosts

#Give it a few minutes and the server set up will be complete.
</code></pre>

<h1>3. Set up a Wireguard VPN tunnel between the client and the middleman.</h1>

<p>We then set up Wireguard on our client.</p>

<p>We begin by installing wireguard on the client depending on what platform we&rsquo;re on.</p>

<pre><code class="bash">add-apt-repository ppa:wireguard/wireguard
apt-get update
apt-get install wireguard-dkms wireguard-tools linux-headers-$(uname -r)
</code></pre>

<p>If you are on Kali Linux, you may have to install <strong>resolvconf</strong> if you don&rsquo;t have it already.</p>

<p>Our Wireguard client config was generated during the middleman install process. The file is named <strong>wg0.conf</strong> and is in the home folder of the middleman. Use scp or whatever other method you prefer to copy it to your client.</p>

<p>I&rsquo;ll rename mine to <strong>vpn0.conf</strong> and move it to <strong>/etc/wireguard/vpn0.conf</strong>.</p>

<p>We finally bring up our VPN interface by running the command:</p>

<pre><code class="bash">sudo wg-quick up vpn0
</code></pre>

<p>We have our Wireguard VPN tunnel in place.</p>

<pre><code class="bash">vpn0      Link encap:UNSPEC  HWaddr 00-00-00-00-00-00-00-00-00-00-00-00-00-00-00-00  
          inet addr:10.200.200.2  P-t-P:10.200.200.2  Mask:255.255.255.255
          UP POINTOPOINT RUNNING NOARP  MTU:1420  Metric:1
          RX packets:110 errors:0 dropped:0 overruns:0 frame:0
          TX packets:241 errors:0 dropped:0 overruns:0 carrier:0
          collisions:0 txqueuelen:1 
          RX bytes:46064 (46.0 KB)  TX bytes:51540 (51.5 KB)
</code></pre>

<p>You should now have a secure VPN connection in place. You can confirm this by checking your IP on sites such as <a href="https://whoer.net/.">https://whoer.net/.</a> Your public IP should be that of the middleman.</p>

<h1>4. Set up a Wireguard VPN tunnel between the middleman and the gate.</h1>

<p>As it stands we have set up a standard VPN connection between the client and the middleman. The next step is now to set up a Wireguard VPN tunnel between the middleman and the gate.</p>

<p>In our scenario the gate will act as the server and the middleman as the client in that tunnel.</p>

<p>During the gate install process, a file named <strong>gate0.conf</strong> was generated and you should find it in the home directory of the gate. Copy it to the middleman using scp.</p>

<p>Move the file to <strong>/etc/wireguard/gate0.conf</strong> on the middleman.</p>

<p>Bring up the VPN interface by running the command:</p>

<pre><code class="bash">wg-quick up gate0
</code></pre>

<p>We now have our Wireguard VPN tunnel between the middleman and the gate in place.</p>

<pre><code class="bash">gate0     Link encap:UNSPEC  HWaddr 00-00-00-00-00-00-00-00-00-00-00-00-00-00-00-00  
          inet addr:10.100.100.2  P-t-P:10.100.100.2  Mask:255.255.255.255
          UP POINTOPOINT RUNNING NOARP  MTU:1420  Metric:1
          RX packets:1 errors:0 dropped:0 overruns:0 frame:0
          TX packets:2 errors:0 dropped:0 overruns:0 carrier:0
          collisions:0 txqueuelen:1 
          RX bytes:92 (92.0 B)  TX bytes:180 (180.0 B)
</code></pre>

<h1>5. Configure policy routing on the middleman to route traffic from the client to the gate.</h1>

<p>We now have all the tunnels in place but traffic from the client will still go out to the internet through the middleman.</p>

<p>Normally all we need to do is modify our routes on the middleman to route traffic from the client to the gate. Wireguard however, using the <strong>wg-quick</strong> tool employs a variant of Rule-based routing using <a href="https://www.linux.org/docs/man8/tc-fw.html" target="_blank"><strong>fwmark</strong></a>.</p>

<p>We are therefore going to configure policy routing to ensure traffic from the client is passed on to the gate by the middleman.</p>

<p>Do the configuration on the middleman as follows:</p>

<pre><code class="bash">echo "1 middleman" &gt;&gt; /etc/iproute2/rt_tables
ip route add 0.0.0.0/0 dev gate0 table middleman 
ip rule add from 10.200.200.0/24 lookup middleman
</code></pre>

<h1>6. Update the middleman gate facing interface (gate0) to allow all traffic from the gate to be allowed in the tunnel.</h1>

<p>Next we need to allow all traffic coming from the gate to be allowed through the tunnel between the middleman and the gate. This is necessary to allow internet traffic that will be passed through the tunnel. Wireguard interfaces are strict in inspecting the origin of traffic that can be allowed to participate in the encrypted tunnel.</p>

<p>On the middleman run the following command:</p>

<pre><code class="bash">wg set gate0 peer &lt;peer_public_key&gt; allowed-ips 0.0.0.0/0
</code></pre>

<p>Note that the <strong>peer_public_key</strong> in the command above can be gotten by running <em>wg show</em> on the middleman and checking the peer key on the <strong>gate0</strong> interface.</p>

<h1>7. Confirm everything works as desired by doing a traceroute to the internet from the client.</h1>

<p>Before celebration, do various tests to confirm traffic from your client is going out through the gate and not the middleman.</p>

<p>Below is a traceroute to 4.2.2.2 confirming my traffic passess through the middleman and goes out through the gate to the internet.</p>

<pre><code class="bash">traceroute to 4.2.2.2 (4.2.2.2), 30 hops max, 60 byte packets
 1  10.200.200.1 (10.200.200.1)  257.422 ms  258.769 ms  258.783 ms
 2  10.100.100.1 (10.100.100.1)  258.772 ms  258.752 ms  258.878 ms
 3  104.131.0.253 (104.131.0.253)  258.877 ms 104.131.0.254 (104.131.0.254)  259.595 ms  260.021 ms
 4  138.197.251.20 (138.197.251.20)  260.028 ms 138.197.251.26 (138.197.251.26)  260.029 ms 138.197.248.36 (138.197.248.36)  260.005 ms
 5  138.197.244.20 (138.197.244.20)  260.430 ms 138.197.244.28 (138.197.244.28)  260.841 ms  261.134 ms
 6  * * lag-103.ear4.Newark1.Level3.net (4.14.218.29)  259.196 ms
 7  * * *
 8  b.resolvers.Level3.net (4.2.2.2)  259.679 ms  260.234 ms  260.117 ms
</code></pre>

<p>Notice that <strong>10.200.200.1</strong> is the the middleman client facing interface IP and <strong>10.100.100.1</strong> is the gate VPN interface IP.</p>

<p>Don&rsquo;t forget to confirm your public IP on sites such as <a href="https://whoer.net/">https://whoer.net/</a> and run a DNS leak test on <a href="http://dnsleak.com/.">http://dnsleak.com/.</a></p>

<p>We are done!</p>

<h1>Selective Routing</h1>

<p>What if we want to allow only certain traffic to go out to the internet through the gate and all the rest to go out through the middleman? The scenario is as below:</p>

<p><img src="/images/chained_selective.png"></p>

<p>Let&rsquo;s put an example scenario to test. We want all our internet traffic to go out through the middleman and not be forwarded to the gate. We however desire to have an exception for traffic to 4.2.2.2, which we want to go out through the gate.</p>

<p>The only thing we will need to do is modify the policy routes we set up on the middleman as follows:</p>

<pre><code class="bash">#Delete the route that forwarded all traffic to the gate
ip route del 0.0.0.0/0 dev gate0 table middleman 

#Add a route to forward 4.2.2.2 to the gate
ip route add 4.2.2.2/32 dev gate0 table middleman 
</code></pre>

<p>Doing traceroute tests should confirm our results.</p>

<pre><code class="bash">#Normal internet traffic
traceroute to 8.8.8.8 (8.8.8.8), 30 hops max, 60 byte packets
 1  10.200.200.1 (10.200.200.1)  256.786 ms  257.063 ms  257.094 ms
 2  104.131.0.253 (104.131.0.253)  257.827 ms 104.131.0.254 (104.131.0.254)  257.830 ms  258.201 ms
 3  138.197.248.52 (138.197.248.52)  258.208 ms  258.196 ms 138.197.248.32 (138.197.248.32)  259.771 ms
 4  138.197.244.34 (138.197.244.34)  259.040 ms  259.439 ms 138.197.244.32 (138.197.244.32)  259.409 ms
 5  72.14.194.62 (72.14.194.62)  259.692 ms  260.107 ms 162.243.191.255 (162.243.191.255)  260.110 ms
 6  * * *
 7  108.170.238.197 (108.170.238.197)  258.633 ms 108.170.238.195 (108.170.238.195)  258.223 ms 108.170.235.179 (108.170.235.179)  258.646 ms
 8  google-public-dns-a.google.com (8.8.8.8)  259.006 ms  259.652 ms  259.583 ms
</code></pre>

<p>Note that our traffic doesn&rsquo;t get routed through the gate interface (<strong>10.100.100.1</strong>).</p>

<pre><code class="bash">#Traffic to 4.2.2.2 
traceroute to 4.2.2.2 (4.2.2.2), 30 hops max, 60 byte packets
 1  10.200.200.1 (10.200.200.1)  256.622 ms  256.789 ms  256.787 ms
 2  10.100.100.1 (10.100.100.1)  257.783 ms  258.323 ms  258.317 ms
 3  * * *
 4  * * *
 5  * * *
 6  * * *
 7  * * *
 8  b.resolvers.Level3.net (4.2.2.2)  259.457 ms  259.321 ms  259.270 ms
</code></pre>

<p>Note that traffic to 4.2.2.2 gets routed through the gate interface (<strong>10.100.100.1</strong>).</p>

<p>Modify the policy routes as need be to suit your needs.</p>

<p>I&rsquo;ll wrap up the VPN series in the next post by showing how to set up a portable VPN solution on a Raspberry PI.</p>

<p>Till then, happy hacking!</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Wireguard VPN: Typical Setup]]></title>
    <link href="http://www.ckn.io/blog/2017/11/14/wireguard-vpn-typical-setup/"/>
    <updated>2017-11-14T07:51:06+03:00</updated>
    <id>http://www.ckn.io/blog/2017/11/14/wireguard-vpn-typical-setup</id>
    <content type="html"><![CDATA[<p>I recently discovered the awesome <a href="https://www.wireguard.com/" target="_blank"><strong>Wireguard</strong></a> VPN tunnel and I was sold. Wireguard is a simple, kernel-based, state-of-the-art VPN that also happens to be ridiculously fast and uses modern cryptographic principles that all other highspeed VPN solutions lack.</p>

<!--more-->


<p>Openvpn used to be my VPN solution of choice but after a few weeks with Wireguard, things changed. See the performance comparision charts done by the Wireguard author, Jason Donenfeld.</p>

<p><img src="/images/wireguard_comparisions.png"></p>

<p>Here are just a few of the reasons why Wireguard blows away the competition:</p>

<ul>
<li>It aims to be as easy to configure and deploy as SSH.</li>
<li>It is capable of roaming between IP addresses (especially useful to prevent dropped connections when you have flaky internet).</li>
<li>Uses state-of-the-art cryptography.</li>
<li>It is meant to be easily implemented in very few lines of code, and easily auditable for security vulnerabilities.</li>
<li>A combination of extremely high speed cryptographic primitives and the fact that WireGuard lives inside the Linux kernel means that secure networking can be very high-speed.</li>
<li>Stealth - does not respond to any unauthenticated packets and both peers become silent when there&rsquo;s no data to be exchanged.</li>
</ul>


<p>Hopefully you too have been sold so let&rsquo;s get into the set up process.</p>

<h1>Set up details</h1>

<p>We will be setting up the typical VPN connection described in the previous post.</p>

<p><img src="/images/typical_vpn.png"></p>

<p>Here&rsquo;s how our set up will look like:</p>

<ul>
<li>An ubuntu 16.04 (x64) VPS as our VPN server (Gateway).</li>
<li>The internet facing interface on the server is eth0.</li>
<li>An ubuntu 16.04 (x64) computer as the client.</li>
<li>We will use 10.200.200.1/24 as the VPN server interface IP.</li>
<li>We will use 10.200.200.2/24 as the VPN client interface IP.</li>
<li>Unbound DNS resolver for added security.</li>
</ul>


<h1>Set up steps</h1>

<ol>
<li>Install WireGuard on the VPN server.</li>
<li>Generate server and client keys.</li>
<li>Generate server and client configs.</li>
<li>Enable WireGuard interface on the server.</li>
<li>Enable IP forwarding on the server.</li>
<li>Configure firewall rules on the server.</li>
<li>Configure DNS.</li>
<li>Set up Wireguard on clients.</li>
</ol>


<h1>1. Install WireGuard on the VPN server</h1>

<p> Comprehensive details on Wireguard installation can be found on the official site <a href="https://www.wireguard.com/install/" target="_blank"><strong>here</strong></a>.
 For our Ubuntu case the process is:</p>

<pre><code class="bash">add-apt-repository ppa:wireguard/wireguard
apt-get update
apt-get install wireguard-dkms wireguard-tools linux-headers-$(uname -r)
</code></pre>

<h1>2. Generate server and client keys</h1>

<p>We will generate the following four files: server_private_key, server_public_key, client_private_key, client_public_key.</p>

<pre><code class="bash">Umask 077
wg genkey | tee server_private_key | wg pubkey &gt; server_public_key
wg genkey | tee client_private_key | wg pubkey &gt; client_public_key
</code></pre>

<h1>3.1 Generate server config</h1>

<p>Create a file called <strong>/etc/wireguard/wg0.conf</strong> on the server and add the following content.</p>

<pre><code class="bash">[Interface]
Address = 10.200.200.1/24
SaveConfig = true
PrivateKey = &lt;insert server_private_key&gt;
ListenPort = 51820

[Peer]
PublicKey = &lt;insert client_public_key&gt;
AllowedIPs = 10.200.200.2/32
</code></pre>

<p><strong>wg0.conf</strong> will result in an interface named <strong>wg0</strong> therefore you can rename the file if you fancy something different.</p>

<p><strong>AllowedIPs = 10.200.200.2/32</strong> provides enhanced security by ensuring that only that a client with the IP <strong>10.200.200.2</strong> and the correct private key will be allowed to authenticate on the VPN tunnel .</p>

<p><strong>ListenPort</strong> is the udp port to listen on. A different one can be used.</p>

<h1>3.2 Generate client config</h1>

<p>Create a file called <strong>wg0-client.conf</strong> on the client and add the following content.</p>

<pre><code class="bash">[Interface]
Address = 10.200.200.2/32
PrivateKey = &lt;insert client_private_key&gt;
DNS = 10.200.200.1

[Peer]
PublicKey = &lt;insert server_public_key&gt;
Endpoint = &lt;insert vpn_server_address&gt;:51820
AllowedIPs = 0.0.0.0/0
PersistentKeepalive = 21
</code></pre>

<p>Similar to the server case, <strong>wg0-client.conf</strong> will result in an interface named wg0-client so you can rename the file if you fancy something different.</p>

<p><strong>AllowedIPs = 0.0.0.0/0</strong> will allow and route all traffic on the client through the VPN tunnel. This can be narrowed down if you only want some traffic to go over VPN.</p>

<p><strong>DNS = 10.200.200.1</strong> will set the DNS resolver IP to our VPN server. This is important to prevent DNS leaks when on the VPN.</p>

<h1>4. Enable the WireGuard interface on the server.</h1>

<p>We will bring up the Wireguard interface on the VPN server as follows:</p>

<pre><code class="bash">chown -v root:root /etc/wireguard/wg0.conf
chmod -v 600 /etc/wireguard/wg0.conf
wg-quick up wg0
systemctl enable wg-quick@wg0.service #Enable the interface at boot
</code></pre>

<p>After this confirm you have a new interface named wg0 by running <strong>ifconfig</strong>.</p>

<pre><code class="bash">wg0       Link encap:UNSPEC  HWaddr 00-00-00-00-00-00-00-00-00-00-00-00-00-00-00-00  
          inet addr:10.200.200.1  P-t-P:10.200.200.1  Mask:255.255.255.0
          UP POINTOPOINT RUNNING NOARP  MTU:1420  Metric:1
          RX packets:0 errors:0 dropped:0 overruns:0 frame:0
          TX packets:0 errors:0 dropped:0 overruns:0 carrier:0
          collisions:0 txqueuelen:1 
          RX bytes:0 (0.0 B)  TX bytes:0 (0.0 B)
</code></pre>

<h1>5. Enable IP forwarding on the server</h1>

<p>Edit the file <strong>/etc/sysctl.conf</strong> and set the following line as:</p>

<pre><code class="bash">net.ipv4.ip_forward=1
</code></pre>

<p>Then also do the following to stop having to reboot the server</p>

<pre><code class="bash">sysctl -p
echo 1 &gt; /proc/sys/net/ipv4/ip_forward
</code></pre>

<h1>6. Configure firewall rules on the server</h1>

<p>We will need to set up a few firewall rules to manage our VPN and DNS traffic.</p>

<p>Track VPN connection</p>

<pre><code class="bash">iptables -A INPUT -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
iptables -A FORWARD -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
</code></pre>

<p>Allowing incoming VPN traffic on the listening port</p>

<pre><code class="bash">iptables -A INPUT -p udp -m udp --dport 51820 -m conntrack --ctstate NEW -j ACCEPT
</code></pre>

<p>Allow both TCP and UDP recursive DNS traffic</p>

<pre><code class="bash">iptables -A INPUT -s 10.200.200.0/24 -p tcp -m tcp --dport 53 -m conntrack --ctstate NEW -j ACCEPT
iptables -A INPUT -s 10.200.200.0/24 -p udp -m udp --dport 53 -m conntrack --ctstate NEW -j ACCEPT
</code></pre>

<p>Allow forwarding of packets that stay in the VPN tunnel</p>

<pre><code class="bash">iptables -A FORWARD -i wg0 -o wg0 -m conntrack --ctstate NEW -j ACCEPT
</code></pre>

<p>Set up nat</p>

<pre><code class="bash">iptables -t nat -A POSTROUTING -s 10.200.200.0/24 -o eth0 -j MASQUERADE
</code></pre>

<p>We also want to ensure that the rules remain persistent across reboots.</p>

<pre><code class="bash">apt-get install iptables-persistent
systemctl enable netfilter-persistent
netfilter-persistent save
</code></pre>

<h1>7. Configure DNS</h1>

<p>A major issue with a lot of VPN set ups is that the DNS is not done well enough. This ends up leaking client connection and location details. A good way to test this is through the great <a href="http://dnsleak.com/">http://dnsleak.com/</a> site.</p>

<p>We are therefore going to ensure that our DNS traffic is secure. After some research I came to the conclusion that the unbound DNS solution is a very good option to use. Some of its merits include:</p>

<ul>
<li>Lightweight and fast</li>
<li>Easy to install and configure</li>
<li>Security oriented</li>
<li>Supports DNSSEC</li>
</ul>


<p>We’ll set it up in a way to counter DNS leakage, more sophisticated attacks like fake proxy configuration, rogue routers and all sorts of MITM attacks on HTTPS and other protocols.</p>

<p>We first do the installation on the server</p>

<pre><code class="bash">apt-get install unbound unbound-host
</code></pre>

<p>We then download the list of Root DNS Servers</p>

<pre><code class="bash">curl -o /var/lib/unbound/root.hints https://www.internic.net/domain/named.cache
</code></pre>

<p>Next we edit the following file <strong>/etc/unbound/unbound.conf</strong></p>

<pre><code class="bash">server:

  num-threads: 4

  #Enable logs
  verbosity: 1

  #list of Root DNS Server
  root-hints: "/var/lib/unbound/root.hints"

  #Use the root servers key for DNSSEC
  auto-trust-anchor-file: "/var/lib/unbound/root.key"

  #Respond to DNS requests on all interfaces
  interface: 0.0.0.0
  max-udp-size: 3072

  #Authorized IPs to access the DNS Server
  access-control: 0.0.0.0/0                 refuse
  access-control: 127.0.0.1                 allow
  access-control: 10.200.200.0/24           allow

  #not allowed to be returned for public internet  names
  private-address: 10.200.200.0/24

  # Hide DNS Server info
  hide-identity: yes
  hide-version: yes

  #Limit DNS Fraud and use DNSSEC
  harden-glue: yes
  harden-dnssec-stripped: yes
  harden-referral-path: yes

  #Add an unwanted reply threshold to clean the cache and avoid when possible a DNS Poisoning
  unwanted-reply-threshold: 10000000

  #Have the validator print validation failures to the log.
  val-log-level: 1

  #Minimum lifetime of cache entries in seconds
  cache-min-ttl: 1800   

  #Maximum lifetime of cached entries
  cache-max-ttl: 14400
  prefetch: yes
  prefetch-key: yes
</code></pre>

<p>I have commented the config file explaining the specific configuration details.</p>

<p>Finally we set some permissions, enable and test the operation on our DNS resolver.</p>

<pre><code class="bash">chown -R unbound:unbound /var/lib/unbound
systemctl enable unbound
</code></pre>

<p>Here are some sample test results</p>

<pre><code class="bash">nslookup www.google.com. 10.200.200.1

    Server:     10.200.200.1
    Address:    10.200.200.1#53

    Non-authoritative answer:
    Name:   www.google.com
    Address: 172.217.7.228

#Testing DNSSEC
unbound-host -C /etc/unbound/unbound.conf -v ietf.org

    [1510648646] libunbound[1739:0] notice: init module 0: validator
    [1510648646] libunbound[1739:0] notice: init module 1: iterator
    ietf.org has address 4.31.198.44 (secure)
    ietf.org has IPv6 address 2001:1900:3001:11::2c (secure)
    ietf.org mail is handled by 0 mail.ietf.org. (secure)
</code></pre>

<p>With that, our server setup is done :)</p>

<h1>8. Set up Wireguard on clients</h1>

<p>We can now finally set up our client.</p>

<p>We begin by installing wireguard on the client depending on what platform we&rsquo;re on. The installation process is the same as the server&rsquo;s.</p>

<pre><code class="bash">add-apt-repository ppa:wireguard/wireguard
apt-get update
apt-get install wireguard-dkms wireguard-tools linux-headers-$(uname -r)
</code></pre>

<p>If you are on Kali Linux, you may have to install <strong>resolvconf</strong> if you don&rsquo;t have it already.</p>

<p>We had already generated the <strong>wg0-client.conf</strong> client config in step <strong>3.2</strong>.
All we need to do is to move it to <strong>/etc/wireguard/wg0-client.conf</strong>.</p>

<p>We finally bring up our VPN interface by running the command:</p>

<pre><code class="bash">sudo wg-quick up wg0-client
</code></pre>

<p>And voila, we have our Wireguard VPN tunnel in place.</p>

<pre><code class="bash">wg0-client Link encap:UNSPEC  HWaddr 00-00-00-00-00-00-00-00-00-00-00-00-00-00-00-00  
          inet addr:10.200.200.2  P-t-P:10.200.200.2  Mask:255.255.255.255
          UP POINTOPOINT RUNNING NOARP  MTU:1420  Metric:1
          RX packets:95 errors:0 dropped:0 overruns:0 frame:0
          TX packets:177 errors:0 dropped:0 overruns:0 carrier:0
          collisions:0 txqueuelen:1 
          RX bytes:14236 (14.2 KB)  TX bytes:31516 (31.5 KB)
</code></pre>

<p>The wg command is a great Wireguard utility that you can use to view connection status.</p>

<pre><code class="bash">sudo wg show

    interface: wg0-client
      public key: FwdTNMXqL46jNhZwkkzWsyR1AIlGX66vRWe1HFSemHw=
      private key: (hidden)
      listening port: 39451
      fwmark: 0xca6c

    peer: +lb7/6Nn8uhlA/6fjT3ivfM5fWKKQ2L+stX+dSq18CI=
      endpoint: 165.227.120.177:51820
      allowed ips: 0.0.0.0/0
      latest handshake: 49 seconds ago
      transfer: 11.41 MiB received, 862.25 KiB sent
      persistent keepalive: every 21 seconds
</code></pre>

<p>You should now have a secure VPN connection in place. You can confirm this by checking your IP on sites such as <a href="https://whoer.net/.">https://whoer.net/.</a></p>

<p>Ensure you also run a DNS leak test on <a href="http://dnsleak.com/.">http://dnsleak.com/.</a></p>

<p>If you want to disconnect from the VPN you have to bring the VPN interface down.</p>

<pre><code class="bash">sudo wg-quick down wg0-client
</code></pre>

<h1>Wrapping up</h1>

<p>There are some points about the use of Wireguard that should be noted.
First, your VPN connection will remain persistent across networks. This means that unless you specifically bring the interface down or shutdown the computer, you will always be on the VPN. Disconnecting and reconnecting to the same or a different network maintains the connection. Also note that a sleep or suspend of the computer will maintain the interface for when the computer becomes active again.</p>

<p>This is mostly a good thing as you&rsquo;ll maintain your VPN connection even when your internet is unstable or you switch networks.</p>

<p>Another thing to note is that you can set up the VPN interface to be persistent across reboots by enabling it as a service.</p>

<pre><code class="bash">sudo systemctl enable wg-quick@wg0-client.service
</code></pre>

<p>This means that you&rsquo;ll be always on VPN from the moment you boot up the computer.</p>

<p>You may also want to add new clients and the way you do this is as follows:</p>

<p>Generate new client keys on the server</p>

<pre><code class="bash">wg genkey | tee new_client_private_key | wg pubkey &gt; new_client_public_key
</code></pre>

<p>Then register them on the server</p>

<pre><code class="bash">wg set wg0 peer &lt;new_client_public_key&gt; allowed-ips &lt;new_client_vpn_IP&gt;/32
</code></pre>

<p>Finally generate the new client config as described in step <strong>3.2</strong> and you can then set up your clients as per step <strong>8</strong>.</p>

<h1>Automation</h1>

<p>I realised that having to go through all the steps when setting up a new VPN server will probably be a tedious process.
I therefore automated the whole process using <a href="https://www.ansible.com/" target="_blank"><strong>Ansible</strong></a>.</p>

<p>You can therefore quickly spin up a new Wireguard VPN on a new VPS in a few minutes.</p>

<p>You can access the details here <a href="https://github.com/iamckn/wireguard_ansible">https://github.com/iamckn/wireguard_ansible</a></p>

<p>Here are some useful links that have guided this post.</p>

<ul>
<li>Official Wireguard site <a href="https://www.wireguard.com/">https://www.wireguard.com/</a></li>
<li>Unbound – Your own DNS Server <a href="https://freedif.org/unbound-your-own-dns-server/">https://freedif.org/unbound-your-own-dns-server/</a></li>
<li>Installing Wireguard <a href="https://research.kudelskisecurity.com/2017/06/07/installing-wireguard-the-modern-vpn/">https://research.kudelskisecurity.com/2017/06/07/installing-wireguard-the-modern-vpn/</a></li>
<li>Wireguard IPv6 set up <a href="https://danrl.com/blog/2016/travel-wifi/">https://danrl.com/blog/2016/travel-wifi/</a></li>
</ul>


<p>The next post will be about how to up a chained Wireguard VPN connection.</p>

<p>Till then, happy hacking!</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[VPNs! An OPSEC Primer]]></title>
    <link href="http://www.ckn.io/blog/2017/11/12/vpns-an-opsec-primer/"/>
    <updated>2017-11-12T12:05:24+03:00</updated>
    <id>http://www.ckn.io/blog/2017/11/12/vpns-an-opsec-primer</id>
    <content type="html"><![CDATA[<p>The internet is a glorious place. This gloriousness does often come with some pitfalls here and there. Being probably the greatest source of information ever, various actors are constantly devicing ways to acquire and make use of all this data.</p>

<!--more-->


<p>Businesses want to mine this data for monetisation purposes through targeted ads and the like. Criminals want it for malicious intent and profit. Governments and intelligence agencies want it for law enforcement and often censure and control. ISPs want it for monitoring, traffic filtering and so on. The list is endless and the motives ranging from good to bad are almost always controversial.</p>

<p>A lot of the internet community is ok with the status quo and unlikely to be overly concerned with all this. Whether they should be, is a debate that has been explored extensively and therefore something this post will not dwell on.</p>

<p>There are however a lot of us that prefer some form of control over our data as we interact with the World Wide Web. There are those who may have reasons to bypass restrictions placed upon their internet activity as prescribed by various entities mainly ISPs. Then there are those whose work demands operational security as they go about their business. These could include <a href="https://en.wikipedia.org/wiki/Red_team" target="_blank"><strong>red teams</strong></a>, law enforcement, activists and even criminals.</p>

<p>Here, I will try to cover the various ways these groups can use <a href="https://en.wikipedia.org/wiki/Virtual_private_network" target="_blank"><strong>virtual private networks</strong></a> and the merits and weakness of each scenario. I will do so by providing a rating of the operational security each scenario affords them.</p>

<h1>Typical internet connection</h1>

<p>Let&rsquo;s begin by describing the typical way most users access the internet.</p>

<p><img src="/images/typical.png"></p>

<p>This is usually the basic internet connection. The user connects to the network through a router which connects them to their ISP and out into the internet.</p>

<h3>OPSEC Rating: <strong>Low</strong></h3>

<ul>
<li>ISP observes all connections between client and the internet</li>
<li>ISP can view all clear text communication</li>
<li>ISP can see all destination and source IP/port info between client and the internet</li>
<li>ISP can block or filter certain types of traffic</li>
</ul>


<p>As can be seen, the typical internet connection places almost all the control with the ISP.</p>

<h1>Typical VPN connection</h1>

<p>A typical VPN setup involves the client connecting to a VPN gateway/provider over an encrypted tunnel. Their internet traffic therefore goes through this tunnel before being directed out to the internet by the VPN gateway/provider.</p>

<p><img src="/images/typical_vpn.png"></p>

<h3>OPSEC Rating: <strong>Medium</strong></h3>

<ul>
<li>ISP can only see destination and source IP/port info between client and VPN Gateway</li>
<li>ISP blocking and filtering of traffic is bypassed by use of the VPN tunnel</li>
<li>No clear text communication as traffic is encrypted in the VPN tunnel</li>
<li>ISP can correlate connection details between the Client and it&rsquo;s other clients</li>
<li>Hoster of VPN Gateway can correlate traffic between Client and the internet</li>
</ul>


<p>This VPN connection will meet the needs of most users. It however fails if the intention is good anonymity.
The first problem is the following scenario: let&rsquo;s say the client wants to anonymously connect to a server owned by another client hosted on the same ISP. They will connect to the VPN Gateway which will then forward them to the server. I hope you see the problem here. First, the ISP can see that the client is connected to the VPN Gateway. Secondly, the ISP can also see the VPN Gateway connecting to the server as this server is owned by another client hosted by them. The ISP can therefore correlate the two connection details even though a VPN gateway was involved.</p>

<p>The second problem is that the VPN Gateway can basically see all the connection details between the client and the internet.</p>

<h1>Chained VPN connection</h1>

<p>To overcome some of the challenges identified in the typical VPN connection, a chained VPN connection can be set up as follows.</p>

<p><img src="/images/chained_vpn.png"></p>

<h3>OPSEC Rating: <strong>Good</strong></h3>

<ul>
<li>ISP can only see destination and source IP/port info between client and first VPN Gateway</li>
<li>ISP blocking and filtering of traffic is bypassed by use of the VPN tunnel</li>
<li>No clear text communication as traffic is encrypted in the VPN tunnel</li>
<li>ISP cannot correlate connection details between the Client and it&rsquo;s other clients</li>
<li>Hoster of VPN gateway one cannot correlate traffic between Client and the internet</li>
<li>Hoster of VPN gateway two can correlate normal internet browsing and traffic to the sensitive network</li>
</ul>


<p>This vpn connection provides relatively good anonymity as opposed to the typical one described earlier. It can be seen that the ISP never sees the connection between the two VPN Gateways and therefore cannot link the client to VPN Gateway Two. As all internet traffic goes out through VPN Gateway Two, VPN Gateway One provides an added layer of security against correlation weaknesses of the typical VPN connection.</p>

<p>It should however be noted that VPN Gateway two could possibly identify the client through correlation of metadata information should the client perform personally identifiable internet activity. A likely scenario where this would occur is the client accessing their personal social media or email accounts (metadata such as urls can give away this information). If the client then uses the same VPN connection for other purposes such as accessing the sensitive network shown in the diagram, VPN Gateway two can correlate this to potentially identify the client.</p>

<h1>Chained VPN connection with selective routing</h1>

<p>A variation of the chained VPN connection could be set up as follows.</p>

<p><img src="/images/chained_selective.png"></p>

<h3>OPSEC Rating: <strong>Good</strong></h3>

<ul>
<li>ISP can only see destination and source IP info between client and first VPN Gateway</li>
<li>ISP blocking and filtering of traffic is bypassed by use of the VPN tunnel</li>
<li>No clear text communication as traffic is encrypted in the VPN tunnel</li>
<li>ISP cannot correlate connection details between client and sensivite network</li>
<li>Hoster of VPN gateway one cannot correlate normal internet browsing and traffic to the sensitive network</li>
<li>Hoster of VPN Gateway two cannot observe Client&rsquo;s normal internet browsing</li>
</ul>


<p>In this setup, normal internet traffic from the client is forwarded directly to the destination by VPN Gateway One and isn&rsquo;t forwarded to VPN Gateway Two. Select traffic that may be considered sensitive is however explicitly forwarded to to VPN Gateway Two.</p>

<p>What this achieves is to ensure that the client&rsquo;s normal traffic such as normal web surfing doesn&rsquo;t go out the same VPN Gateway as traffic they wouldn&rsquo;t want linked to them. VPN Gateway One will only see normal internet traffic while VPN Gateway Two will only see traffic to the sensitive network.</p>

<h1>Takeway notes</h1>

<ul>
<li>The typical vpn connection should suit most users who just need basic anonymity and security.</li>
<li>The two chained options provide improved anonymity.</li>
<li>Use of any of the VPN setups described alone isn&rsquo;t to be assumed to guarantee operational security. Opsec hygiene should be observed and complementary solutions and techniques employed.</li>
<li>An advanced adversary such as a nation state will defeat the setups described with a high likelihood.</li>
</ul>


<h1>Addendum</h1>

<p>I&rsquo;ll be doing a series on how to implement the various setups using my favorite VPN solution, the extremely simple yet fast solution VPN called <a href="https://www.wireguard.com/" target="_blank"><strong>Wireguard</strong></a>.
As part of the series, I&rsquo;ll also detail how to setup the portable Wireguard VPN solution shown below using a Raspberry Pi.</p>

<p><img src="/images/portable_vpn.png"></p>

<p>Here are some useful links related to this post.</p>

<ul>
<li><p>Some great opsec rules from <a href="https://grugq.tumblr.com/post/60463307186/rules-of-clandestine-operation">https://grugq.tumblr.com/post/60463307186/rules-of-clandestine-operation</a></p></li>
<li><p>The awesome Wireguard VPN <a href="https://www.wireguard.com/">https://www.wireguard.com/</a></p></li>
<li><p>My go to network diagram tool <a href="https://www.draw.io/">https://www.draw.io/</a></p></li>
</ul>


<p>A typical VPN connection using Wireguard follows next.</p>

<p>Till next time, happy hacking!</p>
]]></content>
  </entry>
  
</feed>
