<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: radio | the poetry of (in)security]]></title>
  <link href="http://www.ckn.io/blog/categories/radio/atom.xml" rel="self"/>
  <link href="http://www.ckn.io/"/>
  <updated>2016-06-12T12:02:16+03:00</updated>
  <id>http://www.ckn.io/</id>
  <author>
    <name><![CDATA[ck]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[A DIY Listening Device]]></title>
    <link href="http://www.ckn.io/blog/2016/02/16/a-diy-listening-device/"/>
    <updated>2016-02-16T19:51:44+03:00</updated>
    <id>http://www.ckn.io/blog/2016/02/16/a-diy-listening-device</id>
    <content type="html"><![CDATA[<p>I have had a set of Intex Wireless Roaming Headphones lying around the house for a couple of years. They consist of a receiver (headphones) and a separate transmitter. The headphones include an FM receiver powered by two AAA batteries with reset and scan buttons for normal FM operation, and of course volume control. The separate transmitter comes with audio and microphone jack ports and is also powered by two AAA batteries. It also has a DC port as an alternative power source option. Here&rsquo;s how it looks:</p>

<!--more-->


<p><img src="/images/headsets.jpg"></p>

<p>How it works is that you connect your audio source to either the transmitter or the headphones with an aux cable. If you go the transmitter way, to listen to the audio, you press the reset button then the scan button on the headphones. The headphones then receive the audio wireless from the transmitter. That&rsquo;s how I had been using the headphones until I got the HackRF One. What was taking place behind the scenes had to be investigated&hellip;naturally :).</p>

<p>The wireless operation of the headsets provided the first clue. The pressing of the reset button followed by the scan button to get the audio from the transmitter meant that the communication was FM. It also followed that the frequency in use was lower than the normal FM frequency band as the audio was heard immediately after the first scan button press.</p>

<p>It was then time to fire up the HackRF One and confirm this. I therefore opened <strong>gqrx</strong> and tuned to around 87.5MHz. The normal FM band is from 87.5 to 108MHz so I wanted to observe the frequencies just before 87.5MHz.
What remained was to connect an audio source to the transmitter and observe what happened on the gqrx display. What was observed is a strong signal peaking at 86.5MHz as shown below:</p>

<p><img src="/images/transmitter_86_5.png"></p>

<p>Tuning to that frequency and listening to the audio output confirmed that it was the audio from the transmitter being received.
Further investigation revealed that the receiver had a frequency range from 84Mhz to 108MHz. The makers of the headsets used the fact that most recievers are tuned to only receive 87.5MHz as the lower end frequency. The 86.5MHz the headsets use is therefore not received on normal FM receivers otherwise anyone listening to FM radio could listen in.</p>

<p><strong>This is where the fun begins</strong></p>

<p>It was now time to open up the headphones and transmitter to see what the actual circuit boards looked like.
The headphones&#8217; circuit board as expected is just a simple FM receiver that can be tuned to lower frequencies than normal FM receivers.
The interesting circuit board is the one from the transmitter. Its input audio source is one of three options: an audio jack, a microphone jack, or an inbuilt microphone. The option to use is selected using a slide switch. The frequency it transmits on is controlled by a trimmer capacitor set in place using epoxy.</p>

<p>The circuit board was relatively small in size so I separated it from its casing.
I then fashioned a micro-usb power port by patching together the two power cables meant for the triple AAA batteries with the end of a female micro-usb cable.
Finally I cleared away the epoxy on the trimmer capacitor to enable tuning of the transmit frequency :). This is how the end product looks like:</p>

<p><img src="/images/fm_transmitter.jpg" width="400"></p>

<p>I had an old powerbank that had some issues so I decided to re-purpose it. I took out the rechargeable batteries patched them up to the end of a USB cable to fashion a portable battery pack. The charging circuit board would come in handy for recharging the batteries :).</p>

<p>Here&rsquo;s the charging circuit board:</p>

<p><img src="/images/battery_charger.jpg" width="400"></p>

<p>Here&rsquo;s the transmitter hooked up to the rechargeable batteries:</p>

<p><img src="/images/transmitter_and_battery.jpg" width="400"></p>

<p><strong>Testing it out</strong></p>

<p>With the transmitter hooked up to the batteries and gqrx running, it was time to test out tuning to different frequencies.
Adjusting the transmit frequency by turning the trimmer capacitor with a small screw driver works as expected. Here&rsquo;s the frequency adjusted to 88.5MHz, which can be listened to on normal FM receivers.</p>

<p><img src="/images/transmitter_88_5.png"></p>

<p>If we set the slide switch on the transmitter to microphone, the transmitter listens to sounds around it and transmits on the frequency we set. Its frequency response range is 20 - 20000Hz which is the human hearing range, making it optimized to capture human voice :).</p>

<p>You can either use the internal mic on the circuit board or connect an external one to the mic jack to meet your specific needs.</p>

<p>With the micro-usb power cable I added, you can power it using the rechargeable batteries, computer/phone USB ports or the normal USB chargers.
The frequency can be tuned using the trimmer capacitor and listened to on a normal FM receiver, or tuned to frequencies below the FM band to prevent eavesdropping by everyone else.
The range of the transmitter is about 30m, but this could easily be increased by having a retransmitter within range.
The transmitter circuit board is small enough to be easily concealed. Creative ways can also be employed like placing it in a hard disk case as I&rsquo;ve done below :)</p>

<p><img src="/images/transmitter_case_open.jpg" width="200"> <img src="/images/transmitter_case_closed.jpg" width="200"></p>

<p><strong>Can it DOS?</strong></p>

<p>Set the slide switch to audio and you can play audio from your preferred audio source (say your phone) using an aux cable to the frequency you tune the transmitter to. If that frequency is allocated to a radio station, then anyone tuned to that station within 30m listens to what you play (legal issues ensue). With its portable nature, you become a mobile radio station DOSer.</p>

<p>The cost of the headsets is about 10 dollars and for that you can come up with some creative ways of making use of the transmitter. You can continue using the headphones independently with an aux cable or listening to FM radio.</p>

<p>For now I plan on replacing the trimmer capacitor with a more flexible one, and hopefully interface it with a smartphone to provide an interface to control the frequency :).</p>

<p>Till next time, happy hacking!</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[GSM: Sniffing voice traffic]]></title>
    <link href="http://www.ckn.io/blog/2016/01/25/gsm-sniffing-voice-traffic/"/>
    <updated>2016-01-25T06:51:00+03:00</updated>
    <id>http://www.ckn.io/blog/2016/01/25/gsm-sniffing-voice-traffic</id>
    <content type="html"><![CDATA[<h4>I wrap up the GSM series with a walkthrough on how to decrypt voice traffic. Voice is the way most people interact on a telecommunications network and therefore a major componenent of GSM traffic. I&rsquo;ve explained a lot of the background on GSM communication in the previous posts so I&rsquo;ll get right to it.</h4>

<!--more-->


<h4>We will capture the traffic using the <strong>HackRF one</strong> and the call will take place between two Safaricom lines. The capture will take place on the downlink - that is the receiving end of the call. I&rsquo;ll use a Blackberry as the receiving device so that I can easily get the TMSI and Kc.</h4>

<h2><strong>Capturing the traffic</strong></h2>

<h4>I&rsquo;ll speed through a lot of these steps as they are similar to the <a href="https://www.ckn.io/blog/2015/11/29/gsm-sniffing-sms-traffic/" target="_blank"><strong>sniffing SMS traffic scenario</strong></a>. I&rsquo;ll use the  Absolute Radio Frequency Channel Number <a href="http://www.telecomabc.com/a/arfcn.html" target="_blank"><strong>ARFCN</strong></a> in specifying the radio channel. GSM uses ARFCNs to represent the various frequencies the BTS and mobile device communicate on. I&rsquo;ll use the ARFCN instead of the frequency in the commands I&rsquo;ll run for variety as I&rsquo;ve been using frequency in the previous posts.</h4>

<h4>We begin by getting the ARFCN, TMSI and Kc from the Blackberry. On a Blackberry these are readily available from the engineering screen menu. The ARFCN is gotten by navigating to the <strong>Cell Identity</strong> submenu, the TMSI from the <strong>Mobile Identity</strong> submenu and finally the Kc from the <strong>SIM Browser</strong> submenu.</h4>

<h4>The values I get are:</h4>

<h4>ARFCN: <strong>17</strong></h4>

<h4>TMSI: <strong>8D4812F8</strong></h4>

<h4>Kc: <strong>239E4C213612C000</strong></h4>

<h4>I use the airprobe_rtlsdr_capture module of <a href="https://github.com/ptrkrysik/gr-gsm" target="_blank"><strong>gr-gsm</strong></a> to capture the voice traffic. I begin the capture by running the following command:</h4>

<pre><code class="bash">airprobe_rtlsdr_capture.py -a 17 -s 1000000 -g 40 -c voice_capture.cfile -T 150
</code></pre>

<p><strong>-a</strong> is the ARFCN, <strong>-s</strong> the sample rate in Hz, <strong>-g</strong> the gain, <strong>-c</strong> the output file and <strong>-T</strong> the duration of our capture in seconds.</p>

<h4>I then make a call while the capture is in progress.</h4>

<h2><strong>Decoding BCCH</strong></h2>

<h4>As explained in the previous post, in idle mode the phone has to listen on the BCCH to detect traffic to be sent to it.</h4>

<h4>Our aim here is to identify what SDCCH (Standalone Dedicated Control Channel) is used for our call setup.</h4>

<h4>We first start wireshark, monitor the loopback interface and then run the following command:</h4>

<pre><code class="bash">airprobe_decode.py -c voice_capture.cfile -s 1000000 -a 17 -m BCCH -t 0
</code></pre>

<h4><strong>voice_capture.cfile</strong> is the file with the voice traffic we captured.</h4>

<h4>We then search for traffic specific to our TMSI by searching for it in wireshark packet details.</h4>

<h4>we look for the paging request and inspect the Immediate Assignment that follows:</h4>

<p><img src="/images/SDCCH_Search.png"></p>

<h4>Note that it&rsquo;s <strong>SDCCH/8</strong>, <strong>Timeslot 1</strong>.</h4>

<h1><strong>Decoding SDCCH</strong></h1>

<h4>We now need to identify the ciphering mode the BTS tells the phone to use.</h4>

<h4>We restart wireshark on the loopback interface and then run the following command specifying <strong>SDCCH8</strong> and <strong>Timeslot 1</strong>:</h4>

<pre><code class="bash">airprobe_decode.py -c voice_capture.cfile -s 1000000 -a 17 -m SDCCH8 -t 1
</code></pre>

<h4>We look for a Paging Response followed by a Ciphering Mode Command.</h4>

<p><img src="/images/ciphering_mode.png"></p>

<h4>We see that the algorithm in use is <strong>A5/1</strong>.</h4>

<h2><strong>Decoding TCH</strong></h2>

<h4>TCH is the Traffic Channel in GSM and is used to carry voice traffic and data. It could either be full rate TCH/F or half rate TCH/H. You can read up more on it <a href="http://www.rfwireless-world.com/Terminology/GSM-traffic-channel-TCH-FS-HS.html" target="_blank"><strong>here</strong></a>.</h4>

<h4>We now restart wireshark on the loopback interface and run the following command:</h4>

<pre><code class="bash">airprobe_decode.py -c voice_capture.cfile -s 1000000 -a 17 -m SDCCH8 -t 1 -e 1 -k 0x23,0x9E,0x4C,0x21,0x36,0x12,0xC0,0x00
</code></pre>

<h4><strong>-e 1</strong> specifies the algorithm <strong>A5/1</strong>, <strong>-k 0x23,0x9E,0x4C,0x21,0x36,0x12,0xC0,0x00</strong> specifies the <strong>Kc</strong>.</h4>

<h4>On wireshark we first look for the Call Control Setup traffic and we can actually see the calling party number as below.</h4>

<p><img src="/images/cc_setup.png"></p>

<h4>A bit down the capture we should see an Assignment command. We see that the voice call is assigned to Timeslot 7 and the Traffic Channel is full rate (TCH/F).</h4>

<p><img src="/images/call_assignment.png"></p>

<h2><strong>Decoding the voice traffic</strong></h2>

<h4>We can now finally decode the voice traffic by running the following command:</h4>

<pre><code class="bash">airprobe_decode.py -c voice_capture.cfile -s 1000000 -a 17 -m TCHF -t 7 -e 1 -k 0x23,0x9E,0x4C,0x21,0x36,0x12,0xC0,0x00 -d FR -o speech.au.gsm
</code></pre>

<h4><strong>-m TCHF</strong> specifies the traffic channel, <strong>-t 7</strong> the TCH/F timeslot, <strong>-d FR</strong> specifies the voice codec of the channel as full rate, and  <strong>speech.au.gsm</strong> specifies the output file.</h4>

<h4><strong>speech.au.gsm</strong> contains the voice traffic. We convert it to an audio file using toast as follows:</h4>

<pre><code class="bash">toast -d speech.au.gsm
</code></pre>

<h4>We will get a file called <strong>speech.au</strong> which we can play back and listen to the captured voice call :).</h4>

<h2><strong>Alternative method</strong></h2>

<h4>Alternatively one could use the mainstream airprobe modules instead of gr-gsm with the same results.</h4>

<h4>The original modules had issues with the HackRF and later GNU Radio versions. I however did some patching and you can clone the patched version from my Github <a href="https://github.com/iamckn/airprobe" target="_blank"><strong>here</strong></a>.</h4>

<h4>The equivalent commands for the whole process starting from decoding BCCH to decoding voice are:</h4>

<pre><code class="bash">./go.sh voice_capture.cfile 64 0B
./go.sh voice_capture.cfile 64 1S 239E4C213612C00001
./go.sh voice_capture.cfile 64 7T 239E4C213612C00001
toast -d speech.au.gsm 
</code></pre>

<h4>That concludes the GSM radio series for now.</h4>

<h4>Till next time, happy hacking!</h4>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[GSM: Sniffing SMS traffic]]></title>
    <link href="http://www.ckn.io/blog/2015/11/29/gsm-sniffing-sms-traffic/"/>
    <updated>2015-11-29T10:14:48+03:00</updated>
    <id>http://www.ckn.io/blog/2015/11/29/gsm-sniffing-sms-traffic</id>
    <content type="html"><![CDATA[<h4>In the previous <a href="https://www.ckn.io/blog/2015/11/01/sniffing-gsm-traffic/" target="_blank"><strong>post</strong></a>, I explained how GSM traffic can be sniffed with the <a href="https://www.ckn.io/blog/2015/10/11/the-hackrf-one-first-steps/" target="_blank"><strong>HackRF One</strong></a>. GSM traffic carries a lot of information, from system information to the actual voice and data we are familiar with. The traffic that the normal user of a telecommunication network is concerned with is voice and data. With this in mind I&rsquo;ll do a two part series to demonstrate how voice and data can be sniffed using the HackRF. I start with SMS traffic which falls under the data category. Let&rsquo;s get right into it!</h4>

<!--more-->


<h4>I am going to send an SMS from a Safaricom line to an Orange line, capture the traffic over the <a href="https://en.wikipedia.org/wiki/Um_interface" target="_blank"><strong>Um (air) interface</strong></a> and decrypt the data to retrieve the SMS. I own both lines steering clear of the legality issue of decrypting other people&rsquo;s traffic.</h4>

<h2><strong>Identifying the BTS</strong></h2>

<h4>The specific point at which I&rsquo;ll capture the traffic is as it&rsquo;s being sent by the BTS to the Orange line. The technical term for this is the downlink. I therefore need to identify the BTS that my Orange line is connected to. A BTS is uniquely identified using an assigned cell identity (Cell ID). The cell identity combined with the <a href="https://en.wikipedia.org/wiki/Location_area_identity" target="_blank"><strong>location area identity (LAI)</strong></a> which uniquely identifies the country, mobile network and location area code is what we need to get. There are various ways to get this information such as the engineering menu on blackberries. The phone I am using is an android phone and there are several android apps that will give you this information. In my case I use the awesome <a href="https://github.com/SecUpwN/Android-IMSI-Catcher-Detector" target="_blank"><strong>Android IMSI Catcher Detector</strong></a>.</h4>

<h4>Next we sniff the GSM frequencies our mobile operators use and identify the specific frequency the BTS is operating on. Follow the previous <a href="https://www.ckn.io/blog/2015/11/01/sniffing-gsm-traffic/" target="_blank"><strong>post</strong></a> on how to do this. We will accomplish this by searching the traffic being captured on wireshark for the LAI and Cell ID our phone is on until we have a match. In this case the frequency the Orange BTS was operating on is 949.2MHz.</h4>

<h2><strong>Capturing SMS traffic</strong></h2>

<h4>I&rsquo;ll use the airprobe_rtlsdr_capture module of <a href="https://github.com/ptrkrysik/gr-gsm" target="_blank"><strong>gr-gsm</strong></a> to capture the SMS traffic. I begin the capture using the following command:</h4>

<pre><code class="bash">airprobe_rtlsdr_capture.py -f 949200000 -s 1000000 -g 40 -c capture.cfile -T 60
</code></pre>

<p><strong>-f</strong> is the frequency in Hz, <strong>-s</strong> the sample rate in Hz, <strong>-g</strong> the gain, <strong>-c</strong> the output file and <strong>-T</strong> the duration of our capture in seconds.</p>

<h4>I then send an SMS reading &ldquo;This is a demo of GSM decryption&rdquo; to the Orange line.</h4>

<h2><strong>Getting the TMSI and Kc</strong></h2>

<h4>We now have the traffic captured and saved in a file called <strong>capture.cfile</strong>. Before we get into the decryption process, we need some information specific to our SIM card.</h4>

<h4>First we need the Temporary Mobile Subscriber Identity (<strong>TMSI</strong>). This is a random assigned identity assigned to the SIM when it connects to a BTS. Its purpose is to avoid the subscriber from being identified and tracked by eavesdroppers on the air interface as I am trying to do :).</h4>

<h4>We will then need to get the <strong>Kc</strong>, which is the key used to encrypt the traffic between the phone and the BTS over the air. I will get into the details of how the <strong>Kc</strong> is calculated in a later post but for now you can read up on the <strong>A8</strong> algorithm.</h4>

<h4>There are various ways to get the <strong>TMSI</strong> and <strong>Kc</strong>. These guides <a href="http://domonkos.tomcsanyi.net/?p=369" target="_blank"><strong>here</strong></a> and <a href="http://openbsc.osmocom.org/trac/wiki/A5_GSM_AT_tricks" target="_blank"><strong>here</strong></a> are great references.</h4>

<h4>I used AT commands on a connected Samsung S3 mini as follows:</h4>

<pre><code class="bash">AT+CRSM=176,28448,0,0,9
+CRSM: 144,0,0E10EAF30299F2C404
</code></pre>

<h4>The <strong>Kc</strong> from the output is <strong>0E10EAF30299F2C4</strong></h4>

<pre><code class="bash">AT+CRSM=176,28542,0,0,11
+CRSM: 144,0,38256E5136F9702B5D0000
</code></pre>

<h4>The <strong>TMSI</strong> from the output is <strong>38256E51</strong></h4>

<h2><strong>Decoding BCCH</strong></h2>

<h4>BCCH is the Broadcast Control Channel that the BTS uses to communicate system information messages to the mobile device on.</h4>

<h4>In idle mode the phone has to listen on the BCCH to detect traffic to be sent to it.</h4>

<h4>In this case the BTS sends two paging request messages which inform the phone of an incoming SMS.</h4>

<h4>This will then be followed by an Immediate Assignment where an SDCCH is established for the SMS transfer.</h4>

<h4>SDCCH is a Standalone Dedicated Control Channel used for most short transactions, including initial call setup step, registration and SMS transfer.</h4>

<h4>We therefore need to identify what SDCCH is used for the SMS transfer.</h4>

<h4>We first start wireshark and monitor the loopback interface and then run the following command:</h4>

<pre><code class="bash">airprobe_decode.py -c capture.cfile -s 1000000 -f 949200000 -m BCCH -t 0
</code></pre>

<h4><strong>capture.cfile</strong> is the file with the traffic we captured earlier.</h4>

<h4>We then search for traffic specific to our TMSI by searching for it in wireshark packet details.</h4>

<p><img src="/images/tmsi_search.png"></p>

<h4>we look for the two paging requests and inspect the Immediate Assignment that follows:</h4>

<p><img src="/images/sdcch_channel.png"></p>

<h4>Note that it&rsquo;s <strong>SDCCH/8</strong>, <strong>Timeslot 2</strong>.</h4>

<h2><strong>Decoding SDCCH</strong></h2>

<h4>We now need to identify the ciphering mode the BTS tells the phone to use.</h4>

<h4>We restart wireshark on the loopback interface and then run the following command specifying <strong>SDCCH8</strong> and <strong>Timeslot 2</strong>:</h4>

<pre><code class="bash">airprobe_decode.py -c capture.cfile -s 1000000 -f 949200000 -m SDCCH8 -t 2
</code></pre>

<h4>We look for a Paging Response followed by a Ciphering Mode Command.</h4>

<p><img src="/images/cipher_mode.png"></p>

<h4>We see that the algorithm in use is <strong>A5/1</strong> (more on this in a later post).</h4>

<h2><strong>Decrypting the SMS traffic. Finally!</strong></h2>

<h4>We confirm the most recently used <strong>Kc</strong> as shown earlier, then we restart wireshark on the loopback interface and run the following command:</h4>

<pre><code class="bash">airprobe_decode.py -c capture.cfile -s 1000000 -f 949200000 -m SDCCH8 -t 2 -e 1 -k 0x0E,0x10,0xEA,0xF3,0x02,0x99,0xF2,0xC4
</code></pre>

<h4><strong>-e 1</strong> specifies the algorithm <strong>A5/1</strong>, <strong>-k 0x0E,0x10,0xEA,0xF3,0x02,0x99,0xF2,0xC4</strong> specifies the <strong>Kc</strong>.</h4>

<h4>On wireshark we look for the GSM SMS traffic and we can see the text of our SMS :).</h4>

<p><img src="/images/decrypted_sms.png"></p>

<h2><strong>Wrapping up</strong></h2>

<h4>If you&rsquo;ve followed through keenly you&rsquo;ll note that every variable is available readily from just sniffing the traffic except the <strong>Kc</strong>. Everything else is exchanged between the BTS and Mobile device, from the TMSI and BTS details to the data traffic though encrypted. The question becomes how do you get the <strong>Kc</strong> in use by another mobile device? The good people at <a href="https://srlabs.de/decrypting_gsm/" target="_blank"><strong>SRLabs</strong></a> figured out how to crack the A5/1 encryption used and created a tool called <strong>Kraken</strong> and 2TB rainbow tables to find the <strong>Kc</strong>.</h4>

<h4>They also automated a lot of the steps in this post making decryption of gsm voice and data almost trivial.</h4>

<h4>In the next post I&rsquo;ll demonstrate how to decrypt voice traffic :) as I continue explaining GSM communication.</h4>

<h4>Till then, happy hacking.</h4>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Sniffing GSM Traffic]]></title>
    <link href="http://www.ckn.io/blog/2015/11/01/sniffing-gsm-traffic/"/>
    <updated>2015-11-01T09:30:27+03:00</updated>
    <id>http://www.ckn.io/blog/2015/11/01/sniffing-gsm-traffic</id>
    <content type="html"><![CDATA[<h4>I have been playing around with the HackRF for the past couple of weeks and progressively exploring the Radio Frequency spectrum. In this post I&rsquo;ll take you through how to sniff GSM traffic. I&rsquo;ll be specifically monitoring the <a href="https://en.wikipedia.org/wiki/Um_interface" target="_blank"><strong>Um interface</strong></a>. This in the air interface between the Mobile Station (MS) and the Base Transceiver Station (BTS). The MS in this case will be the mobile phone while the BTS is what the phone connects to on the Mobile network. The BTS is usually hosted on towers which you can spot in various locations. Here is what a typical one looks like.</h4>

<!--more-->


<p><img src="/images/BTS.jpg" width="400"></p>

<h4>We first ensure that our HackRF is well set up as detailed in <a href="https://www.ckn.io/blog/2015/10/11/the-hackrf-one-first-steps/" target="_blank"><strong>this earlier post</strong></a>.</h4>

<h4>This setup is tailored to Archlinux but other linux distros closely follow the same process.</h4>

<h4>Next we install <a href="https://github.com/ptrkrysik/gr-gsm" target="_blank"><strong>gr-gsm</strong></a> which consists of gnuradio blocks and tools for receiving GSM transmissions.</h4>

<h3><strong><em>Setting up gr-gsm</em></strong></h3>

<h4>Install libosmocore libraries</h4>

<pre><code class="bash">yaourt -S libosmocore   
</code></pre>

<h4>Install gr-gsm</h4>

<pre><code class="bash">git clone https://github.com/ptrkrysik/gr-gsm
cd gr-gsm
mkdir build
cd build
cmake ..
make
sudo make install
sudo ldconfig
</code></pre>

<h4>Add the path of the gr-gsm blocks to gnuradio</h4>

<pre><code class="bash">nano ~/.gnuradio/config.conf    
    [grc]
    local_blocks_path=/usr/local/share/gnuradio/grc/blocks
</code></pre>

<h4>Move gr-gsm library and python modules to correct location (depends on your environment).</h4>

<pre><code class="bash">sudo mv /usr/local/lib/python2.7/site-packages/grgsm /usr/lib/python2.7/site-packages/
sudo mv /usr/local/lib/libgnuradio-grgsm.so /usr/lib/
</code></pre>

<h4>Next we will set up kalibrate-hackrf which we will use to find what GSM frequencies to sniff on.</h4>

<h3><strong><em>Setting up kalibrate-hackrf</em></strong></h3>

<pre><code class="bash">git clone https://github.com/scateu/kalibrate-hackrf.git
cd kalibrate-hackrf
./bootstrap
./configure
make
sudo make install
</code></pre>

<h3><strong><em>Finding GSM frequencies</em></strong></h3>

<h4>With our prerequisites done, the next step is to find GSM frequencies to listen on. First we consult <a href="http://www.worldtimezone.com/gsm.html" target="_blank"><strong>this site</strong></a> to identify GSM bands used by our local providers.</h4>

<p><img src="/images/gsm_bands.png"></p>

<h4>Let&rsquo;s concentrate on the 900MHz band for now.</h4>

<h4>We use kalibrate-hackrf (kal) to find the frequencies as below:</h4>

<pre><code class="bash">kal -s GSM900 -g 40 -l 40
</code></pre>

<h4><strong>-s</strong> is the band to scan, <strong>-g</strong> is the baseband gain and <strong>-l</strong> the interface gain</h4>

<h4>Let it run for a minute or so and you should get output similar to:</h4>

<p><img src="/images/kal.png"></p>

<h3><strong><em>Sniffing!</em></strong></h3>

<h4>Let us now use gqrx to confirm the frequencies by tuning in to the first one. In this case <strong>936.4MHz</strong></h4>

<p><img src="/images/gqrx_gsm.png"></p>

<h4>We can see that the activity is centered around <strong>936.8MHz</strong> so we adjust accordingly and use that as the frequency.</h4>

<h4>We now start capturing gsm traffic using gr-gsm :). What we do is launch gnuradio companion and open <strong>the airprobe_rtlsdr.grc</strong> block that is part of gr-gsm and run it.</h4>

<pre><code class="bash">gnuradio-companion airprobe_rtlsdr.grc
</code></pre>

<h4>Tune to <strong>936.8MHz</strong> and if all went well we should get data streaming through the console window.</h4>

<p><img src="/images/gr_gsm_traffic.png"></p>

<h4>We finally launch wireshark and monitor the loopback interface to see the decoded traffic :)</h4>

<h4>Here is a sample showing BTS Location details.</h4>

<p><img src="/images/gsm_wireshark.png"></p>

<h4>So what next after sniffing the traffic?</h4>

<h4>In subsequent posts I&rsquo;ll explain and show how to interpret the decoded traffic as I explain how GSM communication takes place.</h4>

<h4>From there I&rsquo;ll demonstrate how to decrypt the traffic. That is where it gets really interesting as we start reading SMS&rsquo;s, listening to calls and so on :).</h4>

<h4>Till then, happy hacking.</h4>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[The HackRF One: First steps]]></title>
    <link href="http://www.ckn.io/blog/2015/10/11/the-hackrf-one-first-steps/"/>
    <updated>2015-10-11T20:18:10+03:00</updated>
    <id>http://www.ckn.io/blog/2015/10/11/the-hackrf-one-first-steps</id>
    <content type="html"><![CDATA[<h4>The wireless world is an area I&rsquo;ve been interested in for a long time. From the the more common applications such as Wi-Fi, bluetooth and FM to the lesser explored such as radar, satellite and GSM, radio frequency is an area I plan to explore extensively. How awesome is the concept of <a href="https://en.wikipedia.org/wiki/Electromagnetic_pulse" target="_blank"><strong>electromagnetic pulses</strong></a> in this age that is driven by electromagnetism. I digress so let me get back on track, there will be several more posts to explore the possibilities.</h4>

<!--more-->


<h4>I recently acquired the awesome <a href="https://greatscottgadgets.com/hackrf/" target="_blank"><strong>HackRF One</strong></a>, a Software Defined Radio peripheral capable of transmission or reception of radio signals from 1 MHz to 6 GHz. I added the <a href="http://www.nooelec.com/store/ham-it-up.html" target="_blank"><strong>Ham It Up v1.3 - RF Upconverter</strong></a> to boost performance in the lower frequency ranges. A telescopic antenna and various connectors complete the setup.</h4>

<h2>The Hardware</h2>

<h4><strong>The HackRF One, telescopic antenna (ANT 500), some connectors and a Ham It Up v1.3 - RF Upconverter</strong></h4>

<p><img src="/images/hackrf_hardware.JPG"></p>

<h2>Software setup</h2>

<h4>I did my setup on archlinux but it should be just as easy on other OS&rsquo;s. Here are the instructions from the <a href="https://github.com/mossmann/hackrf/wiki/Operating-System-Tips" target="_blank"><strong>HackRF Github wiki page</strong></a>.</h4>

<h4>On my system I followed the following steps:</h4>

<h3><strong><em>Installation of various software</em></strong></h3>

<pre><code class="bash">sudo pacman -S hackrf gnuradio-osmosdr gnuradio gnuradio-companion gqrx
yaourt -S gr-fosphor-git
</code></pre>

<h3><strong><em>Creation of udev rules to allow access to the HackRF</em></strong></h3>

<pre><code class="bash">sudo nano /etc/udev/rules.d/52-hackrf.rules
    ATTR{idVendor}=="1d50", ATTR{idProduct}=="6089", SYMLINK+="hackrf-one-%k", MODE="660", TAG+="uaccess"
    ATTR{idVendor}=="1fc9", ATTR{idProduct}=="000c", SYMLINK+="hackrf-dfu-%k", MODE="660", TAG+="uaccess"
</code></pre>

<h3><strong><em>Testing the HackRF</em></strong></h3>

<h4>Plug in the HackRF and run</h4>

<pre><code class="bash">hackrf_info
</code></pre>

<h4>You should get output similar to:</h4>

<pre><code class="bash">Found HackRF board 0:
USB descriptor string: 000000000000000014d463dc2f45a7e1
Board ID Number: 2 (HackRF One)
Firmware Version: 2015.07.2
Part ID Number: 0xa000cb3c 0x0069435b
Serial Number: 0x00000000 0x00000000 0x14d463dc 0x2f45a7e1
</code></pre>

<h4>If you get an error, recheck your udev rules and ensure the HackRF USB connection is ok.</h4>

<h3><strong><em>Updating the SPI Flash Firmware</em></strong></h3>

<h4>Download the latest firmware <a href="https://github.com/mossmann/hackrf/releases/tag/v2015.07.2" target="_blank"><strong>here</strong></a>.</h4>

<pre><code class="bash">wget https://github.com/mossmann/hackrf/releases/download/v2015.07.2/hackrf-2015.07.2.zip
unzip hackrf-2015.07.2.zip 
cd hackrf-2015.07.2/firmware-bin/
hackrf_spiflash -w hackrf_one_usb_rom_to_ram.bin
</code></pre>

<h3><strong><em>Updating the CPLD</em></strong></h3>

<pre><code class="bash">cd ..
hackrf_cpldjtag -x firmware/cpld/sgpio_if/default.xsvf
</code></pre>

<h4>Press the reset button, then run the <strong>hackrf_info</strong> command and confirm the firmware updated successfully.</h4>

<h4>We are then ready to try the &ldquo;Hello World&rdquo; of Software Defined Radio, implementing an FM receiver.</h4>

<h2>An FM Receiver</h2>

<h4>We will use the gnuradio-companion for this.</h4>

<h4>Download and save this <a href="https://raw.githubusercontent.com/rrobotics/hackrf-tests/master/fm_radio/fm_radio_rx.grc" target="_blank"><strong>file</strong></a>, then open it using gnuradio from the terminal.</h4>

<pre><code class="bash">gnuradio-companion fm_radio_rx.grc
</code></pre>

<h4>You can then tune in to your preferred radio frequency and listen in.</h4>

<p><img src="/images/fm_receiver.png"></p>

<h4>With that out of the way, you can try building the FM receiver yourself using the gnuradio-companion.</h4>

<h4>With the HackRF&rsquo;s transmission capability, an FM transmitter can also be implemented. I won&rsquo;t say I haven&rsquo;t thought of jamming certain frequencies in the vicinity, but that would be illegal, wouldn&rsquo;t it :)?</h4>

<h4>In the next post, I&rsquo;ll show how to sniff gsm traffic.</h4>

<h4>Till then, happy hacking :).</h4>
]]></content>
  </entry>
  
</feed>
